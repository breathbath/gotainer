1. Service definitions should work independant from the order of declarations
2. Observers and events could be declared inside a service definition rather than a separate declaration
3. Add possible cleanup functions for services and method to container to release resources after programm exit
4. More explicit description about event and observer declarations in the container config
